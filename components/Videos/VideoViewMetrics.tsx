"use client";

import type React from "react";

interface VideoViewMetricsProps {
  playbackId: string;
}

/** View counts are hidden in the product UI. */
const VideoViewMetrics: React.FC<VideoViewMetricsProps> = () => null;

export default VideoViewMetrics;
