"use client";

import type React from "react";

interface RealtimeViewsComponentProps {
  playbackId: string;
}

/** Live viewer counts are hidden in the product UI. */
export const RealtimeViewsComponent: React.FC<RealtimeViewsComponentProps> =
  () => null;
