"use client";

import type React from "react";

interface ViewsComponentProps {
  playbackId: string;
}

/** View counts are hidden in the product UI. */
export const ViewsComponent: React.FC<ViewsComponentProps> = () => null;
