import type { Observable } from 'rxjs';
import type { Response } from 'express';
import type { DiagnosticsLogger } from '@ceramicnetwork/common';
export declare class SseFeed<TInput> {
    private readonly logger;
    private readonly observable;
    private readonly serializeFn;
    constructor(logger: DiagnosticsLogger, observable: Observable<TInput>, serializeFn: (value: TInput) => string);
    send(res: Response): void;
}
//# sourceMappingURL=sse-feed.d.ts.map