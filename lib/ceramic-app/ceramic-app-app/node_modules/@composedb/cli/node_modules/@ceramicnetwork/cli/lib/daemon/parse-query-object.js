export function parseQueryObject(opts) {
    const typedOpts = {};
    for (const [key, value] of Object.entries(opts)) {
        if (typeof value == 'string') {
            if (value[0] == '{') {
                typedOpts[key] = parseQueryObject(JSON.parse(value));
            }
            else if (value === 'true') {
                typedOpts[key] = true;
            }
            else if (value === 'false') {
                typedOpts[key] = false;
            }
            else if (!isNaN(parseInt(value))) {
                typedOpts[key] = parseInt(value);
            }
            else {
                typedOpts[key] = value;
            }
        }
        else {
            typedOpts[key] = value;
        }
    }
    return typedOpts;
}
//# sourceMappingURL=parse-query-object.js.map