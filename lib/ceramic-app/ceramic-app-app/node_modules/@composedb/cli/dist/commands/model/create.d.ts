import { Command, type CommandFlags } from '../../command.js';
export default class CreateModel extends Command<CommandFlags, {
    content: string;
}> {
    static description: string;
    static args: {
        content: import("@oclif/core/lib/interfaces/parser.js").Arg<string, Record<string, unknown>>;
    };
    run(): Promise<void>;
}
