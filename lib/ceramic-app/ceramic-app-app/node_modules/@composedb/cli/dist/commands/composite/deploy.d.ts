import { Command, type CommandFlags } from '../../command.js';
export default class CompositeDeploy extends Command<CommandFlags, {
    compositePath: string | undefined;
}> {
    static description: string;
    static args: {
        compositePath: import("@oclif/core/lib/interfaces/parser.js").Arg<string | undefined, Record<string, unknown>>;
    };
    run(): Promise<void>;
}
