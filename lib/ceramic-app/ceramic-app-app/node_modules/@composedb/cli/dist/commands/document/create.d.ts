import { Command, type CommandFlags } from '../../command.js';
import { StreamID } from '@ceramicnetwork/streamid';
export default class CreateDocument extends Command<CommandFlags, {
    model: StreamID;
    content: any;
}> {
    static description: string;
    static args: {
        model: import("@oclif/core/lib/interfaces/parser.js").Arg<StreamID | undefined, Record<string, unknown>>;
        content: import("@oclif/core/lib/interfaces/parser.js").Arg<any, Record<string, unknown>>;
    };
    run(): Promise<void>;
}
