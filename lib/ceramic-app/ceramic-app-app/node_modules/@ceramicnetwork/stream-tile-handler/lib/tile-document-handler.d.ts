import { TileDocument } from '@ceramicnetwork/stream-tile';
import { CommitData, StreamConstructor, StreamHandler, StreamReaderWriter, StreamState } from '@ceramicnetwork/common';
export declare class TileDocumentHandler implements StreamHandler<TileDocument> {
    private readonly _schemaValidator;
    constructor();
    get type(): number;
    get name(): string;
    get stream_constructor(): StreamConstructor<TileDocument>;
    applyCommit(commitData: CommitData, context: StreamReaderWriter, state?: StreamState): Promise<StreamState>;
    _applyGenesis(commitData: CommitData, context: StreamReaderWriter): Promise<StreamState>;
    _applySigned(commitData: CommitData, state: StreamState, context: StreamReaderWriter): Promise<StreamState>;
    _applyAnchor(commitData: CommitData, state: StreamState): Promise<StreamState>;
}
//# sourceMappingURL=tile-document-handler.d.ts.map