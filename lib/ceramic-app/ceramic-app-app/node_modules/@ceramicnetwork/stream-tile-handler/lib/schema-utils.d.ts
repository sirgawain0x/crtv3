import { StreamReader } from '@ceramicnetwork/common';
import Ajv from 'ajv';
import { LRUCache } from 'least-recent';
export declare class SchemaValidation {
    readonly validators: LRUCache<string, Ajv>;
    constructor();
    validateSchema(ceramic: StreamReader, content: Record<string, any>, schemaStreamId: string): Promise<void>;
    private _loadSchemaById;
    private _validate;
}
//# sourceMappingURL=schema-utils.d.ts.map