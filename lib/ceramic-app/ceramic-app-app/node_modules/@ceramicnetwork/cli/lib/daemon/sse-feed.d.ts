import type { Response } from 'express';
export declare class ExpectedCloseError extends Error {
}
export declare class SSESink<T> implements UnderlyingSink<T> {
    private readonly res;
    private readonly stringify;
    constructor(res: Response, stringify?: (value: T) => string);
    start(controller: WritableStreamDefaultController): void;
    write(element: T): Promise<void>;
    abort(): void;
    close(): void;
}
//# sourceMappingURL=sse-feed.d.ts.map