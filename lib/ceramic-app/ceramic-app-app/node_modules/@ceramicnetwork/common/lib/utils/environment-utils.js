let IPFS_FLAVOR = process.env.IPFS_FLAVOR || 'rust';
export class EnvironmentUtils {
    static useRustCeramic() {
        switch (IPFS_FLAVOR) {
            case 'go':
                return false;
            default:
                return true;
        }
    }
    static setIpfsFlavor(flavor) {
        IPFS_FLAVOR = flavor;
    }
}
//# sourceMappingURL=environment-utils.js.map